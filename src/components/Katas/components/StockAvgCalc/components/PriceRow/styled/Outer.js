import styled from 'styled-components'

const Outer = styled.div`
    margin: auto;
    width: 30%;
    display: flex;

    .cell{
        margin: 2%;
        border: 2px solid red; 
    }
`

export default Outer