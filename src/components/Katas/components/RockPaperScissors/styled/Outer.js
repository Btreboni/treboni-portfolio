import styled from 'styled-components'

const Outer = styled.div`
    margin-top: 25%
    
    img{
        border-radius: 25%;
    }

    img:hover{
        border-radius: 40%;
    }
`

export default Outer