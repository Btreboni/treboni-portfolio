import styled from 'styled-components'

const Outer = styled.div`
    padding-top: 10px;

    h1{
        font-family: Roboto;
    }

    img {
        margin-top: 10px;
        border-radius: 50%;
    }
`

export default Outer